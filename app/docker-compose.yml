services:
  web:
    image: ${APP_IMAGE}
    container_name: webapp
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}